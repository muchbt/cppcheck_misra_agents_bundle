Read .agents/runtime/chunks/chunk_{chunk_index:03d}.json and use the cppcheck-misra-fix skill.

{strategy_instructions}

For each issue whose rule_id appears in unique_fix_patterns, you MUST use the exact fix approach described there. Do NOT invent alternative fix methods when a pattern is provided.

For issues whose rule_id is NOT in unique_fix_patterns, you MUST still add an inline fix comment in the format `/* fix: <rule_id> — <brief cause and fix method> */`. Prefer the simplest single-line change. Do NOT refactor surrounding code or choose elaborate fixes when a minimal one suffices.

Requirements:
1. Process only the current chunk
2. Inspect only files listed in the chunk unless strictly necessary
3. Apply minimal edits only — change only the lines necessary to resolve each issue; do not reformat, refactor, or restructure surrounding code
4. Do not do unrelated refactors or formatting
5. For every code change, add a brief inline comment at the modified line(s) identifying the fixed issue and the fix method, e.g. /* fix: misra-c2012-11.3 — added explicit cast */
6. Do not infer logic from comments
7. Follow the fix_strategy and per-issue strategy_action/risk fields from the chunk JSON
8. Update:
   - {issue_status_delta_path}
   - {file_change_delta_path}
   - {chunk_result_json_path}
   - {chunk_result_md_path}
9. Record each independent edit point with linked issue keys
10. Perform minimal verification and record the result
11. For every high-risk fixed issue, include risk_level, risk_reason, and review_required_after_fix=true in the result JSON
12. Prefer completing work inside the current workspace
13. If one issue is blocked, record the blocker and continue with other safe issues in this chunk
14. Do not wait indefinitely for environmental side effects; write the blocker into the staging files for this chunk
15. Ask the user only when explicit authorization is required and no safe in-workspace path exists
16. Use only the staging paths listed above for this chunk's output files; do not directly overwrite canonical runtime result files
17. If an issue has "previous_fix_attempted": true, the source line already contains a fix comment for the same rule_id but cppcheck still reports the issue. The previous fix was incomplete or incorrect. You MUST re-analyse the line, remove or update the existing incorrect fix comment, and apply the correct fix. Do NOT skip the issue just because a fix comment exists.
